<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ChessSchool.Capacity</RootNamespace>
    <AssemblyName>ChessSchool.Capacity</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Та же шахматная библиотека, что в GameServer — измеряем реальную стоимость хода. -->
    <PackageReference Include="Gera.Chess" Version="1.2.0" />
  </ItemGroup>

</Project>
